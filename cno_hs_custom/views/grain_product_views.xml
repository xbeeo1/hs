<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="view_grain_product_list" model="ir.ui.view">
            <field name="name">grain.product.list</field>
            <field name="model">grain.product</field>
            <field name="arch" type="xml">
                <list>
                    <field name="product_id"/>
                </list>
            </field>
        </record>

        <!-- Form view -->
        <record id="view_grain_product_form" model="ir.ui.view">
            <field name="name">grain.product.form</field>
            <field name="model">grain.product</field>
            <field name="arch" type="xml">
                <form>
                    <sheet>

                        <group>
                            <field name="product_id"/>
                        </group>

                    </sheet>
                </form>
            </field>
        </record>

        <record id="action_grain_product" model="ir.actions.act_window">
            <field name="name">Grain Product</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">grain.product</field>
            <field name="view_mode">list,form</field>
            <field name="context">{}</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    Create Your First Grain Product...!
                </p>
            </field>
        </record>

        <menuitem id="menu_action_grain_product"
                  name="Grain Product"
                  parent="crm.crm_menu_config"
                  action="action_grain_product"
                  sequence="7"/>

    </data>
</odoo>
